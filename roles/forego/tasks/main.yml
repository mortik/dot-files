---
- name: install forego
  homebrew:
    name: forego
    state: latest
  when: ansible_os_family == 'Darwin'

- name: install forego
  pacman:
    name: forego-git
    update_cache: yes
    state: latest
  become: yes
  become_method: sudo
  when: ansible_os_family == 'Archlinux'
